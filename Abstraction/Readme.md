Abstraction: only required information you have to tell the user
keyword = abstract
The concept of abstraction is explained as providing only the required information to the user, using the keyword "abstract." 
